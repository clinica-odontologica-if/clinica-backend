name: CI - Servico de Profissional

on:
  push:
    branches:
      - '**'
    paths:
      - 'servico-profissional/**'
      - '.github/workflows/profissional-ci.yml'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'servico-profissional/**'
      - '.github/workflows/profissional-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./servico-profissional

    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Executar testes do servico de profissional
        run: mvn clean test
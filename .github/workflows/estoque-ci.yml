name: CI - Servico de Estoque

on:
  push:
    branches:
      - '**'
    paths:
      - 'servico-estoque/**'
      - '.github/workflows/estoque-ci.yml'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'servico-estoque/**'
      - '.github/workflows/estoque-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./servico-estoque

    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Executar testes do servico de estoque
        run: mvn clean test

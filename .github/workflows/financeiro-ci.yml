name: CI - Servico Financeiro

on:
  push:
    branches:
      - '**'
    paths:
      - 'servico-financeiro/**'
      - '.github/workflows/financeiro-ci.yml'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'servico-financeiro/**'
      - '.github/workflows/financeiro-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./servico-financeiro

    steps:
      - name: Checkout do codigo
        uses: actions/checkout@v4

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Executar testes do servico financeiro
        run: mvn clean test